#!/usr/bin/env bash
# =============================================================================
# Shebang：指定 Bash 解释器（推荐写法，提高脚本可移植性）
# =============================================================================

set -euo pipefail
# =============================================================================
# 严格模式（Strict Mode）
# -e : 命令失败立即退出
# -u : 未定义变量视为错误
# -o pipefail : 管道中任一命令失败即视为失败
# 作用：显著提升脚本健壮性，推荐在所有生产脚本中使用
# =============================================================================

# ==================== 配置区 ====================
# 获取动态路径
WORK_DIR="$(cd "$(dirname "$0")" && pwd)"
RAW_DIR="$WORK_DIR/../01_RawData"
QC_DIR="$WORK_DIR/../03_Results"

# 定义样本列表
SAMPLES=(
    "SRR34942226" "SRR34942227" "SRR34942228" "SRR34942229"
    "SRR34942230" "SRR34942231" "SRR34942232" "SRR34942233"
)

# 支持任意数量的下载链接
URLS=(
    "$FASTQ1_URL"
    "$FASTQ2_URL"
    # 可继续添加更多 URL
)

